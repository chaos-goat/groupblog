---
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /tag/{{ tag | slug}}/
layout: authors
---

<h1>Tagged “{{ tag }}”</h1>

<ol>
{% assign item = collections[ tag ] %}
{% for post in item | reverse %}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
{% endfor %}
</ol>